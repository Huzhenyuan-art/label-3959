<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OrderMapper">

    <sql id="orderBaseColumns">
        o.id AS order_id,
        o.user_id,
        o.total_amount,
        o.status,
        o.remark,
        o.coupon_id,
        o.discount_amount,
        o.address_id,
        o.receiver_name,
        o.receiver_phone,
        o.receiver_address,
        o.version,
        o.created_time,
        o.updated_time
    </sql>

    <sql id="userJoinColumns">
        u.username,
        u.email AS user_email
    </sql>

    <sql id="couponJoinColumn">
        ct.name AS coupon_name
    </sql>

    <sql id="orderStatusLabel">
        CASE o.status
            WHEN 0 THEN '待支付'
            WHEN 1 THEN '已支付'
            WHEN 2 THEN '已发货'
            WHEN 3 THEN '已完成'
            WHEN 4 THEN '已取消'
            WHEN 5 THEN '退款中'
        END AS status_label
    </sql>

    <sql id="orderItemColumns">
        oi.id AS item_id,
        oi.product_id,
        oi.product_name,
        p.category AS product_category,
        oi.quantity,
        oi.price AS item_price,
        (oi.price * oi.quantity) AS subtotal
    </sql>

    <sql id="orderBaseJoins">
        FROM `order` o
        LEFT JOIN user u ON o.user_id = u.id AND u.deleted = 0
        LEFT JOIN user_coupon uc ON o.coupon_id = uc.id
        LEFT JOIN coupon_template ct ON uc.template_id = ct.id
    </sql>

    <sql id="orderItemJoins">
        LEFT JOIN order_item oi ON oi.order_id = o.id
        LEFT JOIN product p ON oi.product_id = p.id
    </sql>

    <!-- 订单列表结果映射 -->
    <resultMap id="OrderDetailMap" type="com.example.demo.dto.OrderDetailDTO">
        <id property="id" column="order_id"/>
        <result property="userId" column="user_id"/>
        <result property="username" column="username"/>
        <result property="userEmail" column="user_email"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="statusLabel" column="status_label"/>
        <result property="remark" column="remark"/>
        <result property="couponId" column="coupon_id"/>
        <result property="discountAmount" column="discount_amount"/>
        <result property="couponName" column="coupon_name"/>
        <result property="addressId" column="address_id"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="receiverAddress" column="receiver_address"/>
        <result property="version" column="version"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
    </resultMap>

    <!-- 含明细的订单详情结果映射 -->
    <resultMap id="OrderDetailWithItemsMap" type="com.example.demo.dto.OrderDetailDTO" extends="OrderDetailMap">
        <!-- 一对多：订单明细 -->
        <collection property="items" ofType="com.example.demo.dto.OrderDetailDTO$OrderItemDTO">
            <id property="id" column="item_id"/>
            <result property="productId" column="product_id"/>
            <result property="productName" column="product_name"/>
            <result property="productCategory" column="product_category"/>
            <result property="quantity" column="quantity"/>
            <result property="price" column="item_price"/>
            <result property="subtotal" column="subtotal"/>
        </collection>
    </resultMap>

    <!--
        分页查询订单（联查用户信息）
        演示：多表 JOIN + 动态条件 + MyBatis Plus 分页插件自动处理 count 和 limit
    -->
    <select id="selectOrderPage" resultMap="OrderDetailMap">
        SELECT
            <include refid="orderBaseColumns"/>,
            <include refid="userJoinColumns"/>,
            <include refid="orderStatusLabel"/>,
            <include refid="couponJoinColumn"/>
        <include refid="orderBaseJoins"/>
        <where>
            <if test="username != null and username != ''">
                AND u.username LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="status != null">
                AND o.status = #{status}
            </if>
            <if test="userId != null">
                AND o.user_id = #{userId}
            </if>
            <if test="createdTimeStart != null and createdTimeStart != ''">
                AND o.created_time >= #{createdTimeStart}
            </if>
            <if test="createdTimeEnd != null and createdTimeEnd != ''">
                AND o.created_time &lt;= #{createdTimeEnd}
            </if>
        </where>
        ORDER BY o.created_time DESC
    </select>

    <!--
        查询订单详情（联查用户 + 一对多明细 + 商品信息）
        演示：多表 JOIN 一对多映射
    -->
    <select id="selectOrderDetail" resultMap="OrderDetailWithItemsMap">
        SELECT
            <include refid="orderBaseColumns"/>,
            <include refid="userJoinColumns"/>,
            <include refid="orderStatusLabel"/>,
            <include refid="couponJoinColumn"/>,
            <include refid="orderItemColumns"/>
        <include refid="orderBaseJoins"/>
        <include refid="orderItemJoins"/>
        WHERE o.id = #{id}
        ORDER BY oi.id
    </select>

</mapper>
